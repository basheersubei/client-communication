<!DOCTYPE html>
<html lang="en" class="no-js">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"> 
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
  <title>Phi Progress Page</title>
  <meta name="description" content="Phi Progress Page" />
  <link rel="shortcut icon" href="../favicon.ico">
</head>
<body>
  <div class="container">
    <header class="clearfix">
      <span>Phi</span>
      <h1>Progress Page</h1>
    </header> 
    <h2>Project Name: <%= @user.project_name %> </h2>
    <h2>Project Deadline: <%= @user.project_deadline.strftime("%d/%m/%y") %> </h2>
    <div class="main">
      <ul class="cbp_tmtimeline">

        <% @posts.each_with_index do |post, index| %>
        Update: <%= index %>
          <% if post.updated_at > post.created_at %>
            <% date = post.updated_at %>
          <% else %>
            <% date = post.created_at %>
          <% end %>
          <li>
            <time class="cbp_tmtime" datetime="<%= date.to_s %>" >
              <span><%= date.strftime("%d/%m/%y") %></span> 
              <span><%= date.strftime("%I:%M %p") %></span>
            </time>
            <div class="cbp_tmlabel">
              <h2><%= post.title %></h2>
              <p><%= post.content %></p>

              <% if post.photo.file? %>
                <p>
                  Photo attachment: <br>
                  <%= image_tag post.photo.url(:thumb) %>
                </p>

              <% end %>


              <% post.comments.each do |comment| %>
                <p>
                  On <%= comment.created_at.strftime("%d/%m/%y") + " at " + comment.created_at.strftime("%I:%M %p") %>, <%= comment.commenter %> said:<br>
                  <%= comment.content %>

                  <% if comment.photo.file? %>
                    <br>
                    <%= image_tag comment.photo.url(:thumb) %>
                  <% end %>
                </p>
              <% end %>

              <p>
                Add a comment:
                <%= form_for :comment, :url => feedback_path do |f| %>

                  <%= f.text_area :content %><br>
                  <%= f.file_field :photo %><br>
                  <%= f.hidden_field :post_id, :value => post.id %>
                  <%= f.hidden_field :url_hash, :value => @user.url_hash %>
                  <%= f.hidden_field :commenter, value: @user.name %>
                  <%= f.submit %>

                <% end %>
              </p>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</body>
</html>